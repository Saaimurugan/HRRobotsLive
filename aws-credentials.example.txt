# AWS Credentials Configuration
# 
# IMPORTANT: This is a template file. 
# Copy this to 'aws-credentials.txt' and replace with your actual credentials.
# NEVER commit 'aws-credentials.txt' to version control!
#
# To use:
# 1. Copy this file: cp aws-credentials.example.txt aws-credentials.txt
# 2. Edit aws-credentials.txt with your actual AWS credentials
# 3. Keep aws-credentials.txt in .gitignore (already configured)

AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here
AWS_DEFAULT_REGION=us-east-1

# How to get AWS credentials:
# 1. Log in to AWS Console
# 2. Go to IAM > Users
# 3. Create a new user or select existing user
# 4. Go to Security Credentials tab
# 5. Create Access Key
# 6. Copy and paste the values above

# Required IAM Permissions:
# - AWSLambdaFullAccess
# - AmazonDynamoDBFullAccess
# - AmazonS3FullAccess
# - AmazonAPIGatewayAdministrator
# - CloudWatchLogsFullAccess

# Security Best Practices:
# - Use IAM roles in production instead of access keys
# - Rotate credentials regularly (every 90 days)
# - Enable MFA on your AWS account
# - Use least-privilege principle
# - Never share credentials
# - Store credentials in AWS Secrets Manager for production
